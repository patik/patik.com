import css from '../BlogIndex.module.css'

export default function Intro() {
    return (
        <section className={css.intro}>
            <h1 className={css.introTitle}>Blog</h1>
        </section>
    )
}
